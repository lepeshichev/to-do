
create table if not exists users (
    id int primary key,
    username text,
    password text,
    isManager boolean
);

create table if not exists tasks (

);

create table if not exists archive (

)